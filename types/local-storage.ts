export enum LocalStorageKeys {
    youtubeUrl = 'youtubeUrl',
    playbackTime = 'playbackTime',
    playerState = 'playerState',
}

export enum LocalStoragePlayerState {
    playing = 'playing',
    paused = 'paused',
}
